<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div
      class="main bg-zinc-900 w-full min-h-screen p-10 flex flex-col gap-5 text-white relative"
    >
      <%- include('partials/header') %>
      
      <form class="space-y-3" action="/create" method="post">
        <input
          class="bg-zinc-800 w-full rounded-lg p-2 block outline-none"
          type="text"
          placeholder="Title goes here"
          name="title"
        />
        <textarea
          class="bg-zinc-800 w-full rounded-lg p-2 block outline-none resize-none"
          placeholder="Description goes here"
          name="description"
        ></textarea>
        <input
          class="bg-blue-600 px-4 py-2 cursor-pointer rounded-lg"
          type="submit"
          value="Create Task"
        />
      </form>

      <div class="tasks mt-5 flex flex-col gap-5">
        <h2 class="text-2xl text-white">Created Tasks</h2>
        <div class="allTasks flex flex-wrap gap-5">
          <% if(tasks.length>0){ tasks.forEach((task)=>{ %>
          <div
            class="task bg-zinc-800 min-w-72 rounded-lg px-4 py-3 flex flex-col gap-3"
          >
            <h3 class="text-white tracking-tighter"><%= task %></h3>
            <div class="flex justify-between items-center">
              <a class="inline-block text-blue-600" href="/tasks/<%= task %>"
                >Read More</a
              >
              <div class="flex items-center gap-3">
                <a class="inline-block text-zinc-600" href="/edit/<%= task %>"
                  >edit</a
                >
                <a class="text-red-500" href="/delete/<%= task %>">delete</a>
              </div>
            </div>
          </div>

          <% }) } else { %>
          <h3>No tasks found</h3>
          <% } %>
        </div>
      </div>
      
      <%- include('partials/footer') %>
    </div>
  </body>
</html>
