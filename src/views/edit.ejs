<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div
      class="main bg-zinc-900 w-full min-h-screen p-10 flex flex-col gap-5 text-white"
    >
      <%- include('partials/header') %>
      
      <a class="inline-block text-blue-600" href="/"
        >&#8592; Go back to home
      </a>
      <form class="space-y-3" action="/update/<%= taskName %>" method="post">
        <input
          class="bg-zinc-800 w-1/3 rounded-lg p-2 block outline-none"
          type="text"
          placeholder="Previous Title"
          name="previousTitle"
          readonly
          value="<%= taskName %>"
        />
        <input
          class="bg-zinc-800 w-1/3 rounded-lg p-2 block outline-none"
          type="text"
          placeholder="New Title"
          name="newTitle"
        />
        <textarea
          class="bg-zinc-800 w-2/3 rounded-lg p-2 block outline-none resize-none"
          placeholder="Description goes here"
          name="description"
        >
<%= taskDescription %></textarea
        >
        <input
          class="bg-yellow-600 px-4 py-2 cursor-pointer rounded-lg text-black"
          type="submit"
          value="Update Task"
        />
      </form>

      <%- include('partials/footer') %>
    </div>
  </body>
</html>
